import { DynamicInsertDirective } from './dynamic-insert.directive';

describe('DynamicInsertDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicInsertDirective();
    expect(directive).toBeTruthy();
  });
});
